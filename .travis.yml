language: julia
os:
  - linux
julia:
  - '1.0'
env:
  global:
    # Tell PyCall.jl to use python executable in virtualenv:
    PYTHON=python
addons:
  apt:
    packages:
      - python3-pip
before_install:
  - pip3 install --quiet --upgrade --user tox
script:
  - tox
notifications:
  email: false
