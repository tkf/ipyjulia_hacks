language: julia
os:
  - linux
julia:
  - '1.0'
addons:
  apt:
    packages:
      - python3-pip
before_install:
  - pip3 install --quiet --upgrade --user tox
script:
  - export PYTHON=${PWD}/.tox/py3/bin/python
  - tox
notifications:
  email: false
